model_config:
  model_name: mlp
  patch_size: 40
  output_size: 2
  hidden_sizes:
  - 128
  dropout: 0.2
  activation: relu
  batch_norm: true
train_config:
  epochs: 100
  batch_size: 32
  lr_max: 0.001
  weight_decay: 0.01
  optimizer: Adam
  scheduler: cosine
  save_freq: 10
  num_workers: 4
