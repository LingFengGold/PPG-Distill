distillation:
  alpha: 0.5
  beta: 0.5
  temperature: 4.0
  use_feature_distill: true
  feature_loss_weight: 1.0

train_config:
  epochs: 100
  batch_size: 32
  lr_max: 0.001
  weight_decay: 0.01
  optimizer: Adam
  scheduler: cosine
  save_freq: 10
  num_workers: 4 