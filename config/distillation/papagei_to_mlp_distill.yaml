distillation:
  alpha: 0.3
  beta: 0.7
  temperature: 4.0
  
  use_feature_distill: true
  feature_loss_weight: 1.0

train_config:
  epochs: 100
  batch_size: 32
  lr_max: 0.001
  optimizer: "AdamW"
  weight_decay: 0.01
  scheduler: "cosine"
  
  save_freq: 10
  
  patience: 15
  min_delta: 0.001 